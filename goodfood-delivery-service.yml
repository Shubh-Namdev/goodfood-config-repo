server:
  port: 8085

spring:
  application:
    name: goodfood-delivery-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus