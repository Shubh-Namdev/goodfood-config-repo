server:
  port: 9000

spring:
  application:
    name: goodfood-auth-server

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
