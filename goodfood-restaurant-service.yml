server:
  port: 8084

spring:
  application:
    name: goodfood-restaurant-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus